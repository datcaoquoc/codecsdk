export declare function generateUUIDv4(): string;
export declare function _isSameDomain(urlPage: string, urliv: string): boolean;
/**
 * Lấy base URL của SDK đang chạy để load các plugin động từ cùng một nguồn (CDN hoặc local)
 * @returns {string} Base URL (không có dấu / ở cuối)
 */
export declare function _getSDKBaseUrl(): string;
